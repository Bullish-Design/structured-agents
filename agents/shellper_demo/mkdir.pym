from grail import Input

dir_name: str = Input("dir_name")

try:
    result = {
        "dir_name": dir_name,
        "created": True,
    }
except Exception as exc:
    result = {"error": str(exc)}

result
