from grail import Input

folder: str = Input("folder")

try:
    result = {
        "folder": folder,
        "cwd": f"/demo/workspace/{folder}",
    }
except Exception as exc:
    result = {"error": str(exc)}

result
